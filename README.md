# TemizStok – yayın dosyaları

Bu repo otomatik üretilir (`mvskargo/temizstok` → `npm run deploy`). Hostinger'daki cron, `VERSION` değiştiğinde içeriği `public_html` altına kopyalar.
Doğrudan düzenlemeyin; kaynak repo üzerinden değişiklik yapın.
